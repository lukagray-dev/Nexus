<?xml version="1.0" encoding="utf-8"?>
<!-- Feature panel background (Desktop match) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/panel_background" />
    <corners
        android:topLeftRadius="@dimen/corner_radius_large"
        android:topRightRadius="@dimen/corner_radius_large" />
    <stroke
        android:width="@dimen/border_width"
        android:color="@color/border_primary" />
</shape>
